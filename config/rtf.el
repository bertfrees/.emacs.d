;; -----------------------------------------------------------------------------
;; RTF
;; -----------------------------------------------------------------------------

(require-package 'rtf-mode 'rtf-mode)

(add-to-list 'auto-mode-alist '("\\.rtf\\'" . rtf-mode))
