;; -----------------------------------------------------------------------------
;; GO
;; -----------------------------------------------------------------------------

(require-package 'go-mode 'go-mode)

(add-to-list 'auto-mode-alist '("\\.go\\'" . go-mode))
